import customarithmetic;

public class Calc {
    public static String makeOneOperation(String left_operand, String operation, String right_operand) {
        float result;
        left_operand.replaceAll("$", "-");
        right_operand.replaceAll("$", "-");

        if (operation.equals("+")) {
            Integer
        }
    }
}